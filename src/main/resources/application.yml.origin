spring:
  datasource:
    driver-class-name:
    url:
    username:
    password:
  jpa:
    properties: 
        hibernate:
            dialect:
            show_sql: true
  sql:
    init:
      mode: 
  mvc:
    hiddenmethod:
      filter:
        enabled: true
user:
    datastorage:
